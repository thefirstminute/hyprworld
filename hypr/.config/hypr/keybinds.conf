source = ~/.config/hypr/vars.conf
# See https://wiki.hypr.land/Configuring/Keywords/
# Example binds, see https://wiki.hypr.land/Configuring/Binds/ for more

## Important Commands:
bindd = CTRL ALT, Delete, exit Hyprland | Quit logout, exec, hyprctl dispatch exit 0
bindd = $mod, RETURN, Terminal, exec, $terminal

# bindu is supposed to run a command no matter what else is going on...gives error
# bindu = $sysMod, Q, exec, $terminal

##########################   MESSING AROUND    ################### {{{

# hyprctl notify -1 1200 "rgb(d20f39)" "fuckko"

# bind = $sysMod, PERIOD, exec, uwsm app -- $editor

bindd = $sysMod, S, Sleepy Time, exec, systemctl suspend

bind = $sysMod, W, exec, ~/.config/hypr/scripts/wall-this
bind = $ctrlMod, W, exec, ~/.config/hypr/scripts/wallz


# interesting: firefox does a nice full sreen, brave will tile, thorium floats
# bind = $sysMod, PERIOD, exec, firefox --app=https://example.com
# bind = $sysMod, COMMA, exec, brave --app=https://example.com --kiosk --start-fullscreenm
# bind = $sysMod, PERIOD, exec, thorium-browser --app=https://example.com


# bind = SUPER, PERIOD, alterzorder, b

# bind = $sysMod, period, exec, thorium-browser --app=https://grok.com # bind = $sysMod, COMMA, exec, hyprctl notify -1 1200 $colWarn "fuckko"

## }}}
##########################   LAUNCHING STUFF   ################### {{{
# Edit Common Files:
bind = $launchMod, H, exec, $editor ~/.config/hypr/hyprland.conf
# bind = $launchMod, T, workspace, 11
bind = $launchMod, T, exec, $terminal --title "DOME" -e nvim ~/Documents/todo.md

bindd = $sysMod, SLASH, Show Commands, exec, ~/.config/hypr/scripts/keybind-search
bindd = $sysMod, B, Connect Bose Headphones, exec, ~/.config/hypr/scripts/bluetooth-toggle
bindd = $sysMod, P, Toggle Power Saver, exec, ~/.config/hypr/scripts/powersaver


## Main/Quickies:
bindd = $mod, T, Show Time, exec, ~/.config/hypr/scripts/show_time

bindd = $mod, E, Files, exec, $fileManager
bindd = $mod, B, Web Browser | Gnarls FAK www,  exec, $webBrowser


## Rofi Stuff:
bindd = $mod, W, Find Open Windows, exec, rofi -show window
bindd = $mod, SPACE, Apps, exec, rofi -show combi -modes combi -combi-modes "window,drun"
# bindd = $mod, S, Search, exec, rofi -dmenu -p "Search " | xargs -I{} xdg-open https://search.brave.com/search?q={}
# bindd = $mod, S, Search, exec, rofi -dmenu -theme ~/.config/rofi/search.rasi -p "" | xargs -I{} xdg-open "https://search.brave.com/search?q={}"
bindd = $mod, S, Search, exec, sh -c 'q="$(rofi -dmenu -theme ~/.config/rofi/search.rasi -p "")"; [ -n "$q" ] && xdg-open "https://search.brave.com/search?q=${q// /+}"'


## Common Programs/Apps:
bindd = $launchMod, D, FreeCAD, $launch_focus "freecad" "env QT_QPA_PLATFORM=xcb freecad" "18"
# bind = $launchMod, D, exec, env QT_QPA_PLATFORM=xcb freecad 


##  WebApps
bindd = $launchMod, X, Grok, $launch_focus "grok" "thorium-browser --app=https://grok.com" "18"
bindd = $launchMod, P, Perplexity, $launch_focus "Perplexity" "thorium-browser --app=https://perplexity.com" "18"
bindd = $launchMod, G, Gemini, $launch_focus "gemini" "thorium-browser --app=https://gemini.google.com" "18"
bindd = $launchMod, C, Chat GPT, $launch_focus "ChatGPT" "thorium-browser --app=https://chatgpt.com" "18"
bindd = $launchMod, M, Copilot, $launch_focus "Copilot" "thorium-browser --app=https://copilot.microsoft.com" "18"
# bindd = $launchMod, Y, Youtube, $launch_focus "youtube" "thorium-browser --app=https://youtube.com"
bindd = $launchMod, Y, Youtube, $launch_focus "freetube" "freetube --app=freetube"
bindd = $launchMod, I, Hyprland Wiki, $launch_focus "Hyprland Wiki" "thorium-browser --app=https://wiki.hypr.land"

## }}}
##########################   WORKSPACE BINDS   ################### {{{

# Switch workspaces
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9
bind = $mod, 0, workspace, 10

bind = $mod, Y, workspace, 11
bind = $mod, U, workspace, 12
bind = $mod, I, workspace, 13
bind = $mod, O, workspace, 14
bind = $mod, P, workspace, 15
bind = $mod, BRACKETLEFT, workspace, 16
bind = $mod, BRACKETRIGHT, workspace, 17
bind = $mod, SEMICOLON, workspace, 18
bind = $mod, APOSTROPHE, workspace, 19

# Move active window to a workspace
bind = $winMod, 1, movetoworkspacesilent, 1
bind = $winMod, 2, movetoworkspacesilent, 2
bind = $winMod, 3, movetoworkspacesilent, 3
bind = $winMod, 4, movetoworkspacesilent, 4
bind = $winMod, 5, movetoworkspacesilent, 5
bind = $winMod, 6, movetoworkspacesilent, 6
bind = $winMod, 7, movetoworkspacesilent, 7
bind = $winMod, 8, movetoworkspacesilent, 8
bind = $winMod, 9, movetoworkspacesilent, 9
bind = $winMod, 0, movetoworkspacesilent, 10

bind = $winMod, Y, movetoworkspacesilent, 11
bind = $winMod, U, movetoworkspacesilent, 12
bind = $winMod, I, movetoworkspacesilent, 13
bind = $winMod, O, movetoworkspacesilent, 14
bind = $winMod, P, movetoworkspacesilent, 15
bind = $winMod, BRACKETLEFT, movetoworkspacesilent, 16
bind = $winMod, BRACKETRIGHT, movetoworkspacesilent, 17
bind = $winMod, SEMICOLON, movetoworkspacesilent, 18
bind = $winMod, APOSTROPHE, movetoworkspacesilent, 19

# Visual for workspace (maybe get rid of this if you add a bar)
bind = $mod, 1, exec, hyprctl notify 2 $msg_short $colInfo "fontsize:$msg_font_size 1"
bind = $mod, 2, exec, hyprctl notify 2 $msg_short $colInfo "fontsize:$msg_font_size 2"
bind = $mod, 3, exec, hyprctl notify 2 $msg_short $colInfo "fontsize:$msg_font_size 3"
bind = $mod, 4, exec, hyprctl notify 2 $msg_short $colInfo "fontsize:$msg_font_size 4"
bind = $mod, 5, exec, hyprctl notify 2 $msg_short $colInfo "fontsize:$msg_font_size 5"
bind = $mod, 6, exec, hyprctl notify 2 $msg_short $colInfo "fontsize:$msg_font_size 6"
bind = $mod, 7, exec, hyprctl notify 2 $msg_short $colInfo "fontsize:$msg_font_size 7"
bind = $mod, 8, exec, hyprctl notify 2 $msg_short $colInfo "fontsize:$msg_font_size 8"
bind = $mod, 9, exec, hyprctl notify 2 $msg_short $colInfo "fontsize:$msg_font_size 9"
bind = $mod, 0, exec, hyprctl notify 2 $msg_short $colInfo "fontsize:$msg_font_size 10"

bind = $mod, Y,            exec, hyprctl notify 2 $msg_short $colInfo "fontsize:$msg_font_size YRU Here?"
bind = $mod, U,            exec, hyprctl notify 2 $msg_short $colInfo "fontsize:$msg_font_size Upload"
bind = $mod, I,            exec, hyprctl notify 2 $msg_short $colInfo "fontsize:$msg_font_size Code"
bind = $mod, O,            exec, hyprctl notify 2 $msg_short $colInfo "fontsize:$msg_font_size Online"
bind = $mod, P,            exec, hyprctl notify 2 $msg_short $colInfo "fontsize:$msg_font_size PR0"
bind = $mod, BRACKETLEFT,  exec, hyprctl notify 2 $msg_short $colInfo "fontsize:$msg_font_size Draft"
bind = $mod, BRACKETRIGHT, exec, hyprctl notify 2 $msg_short $colInfo "fontsize:$msg_font_size Utils"
bind = $mod, SEMICOLON,    exec, hyprctl notify 2 $msg_short $colInfo "fontsize:$msg_font_size Nota"
bind = $mod, APOSTROPHE,   exec, hyprctl notify 2 $msg_short $colInfo "fontsize:$msg_font_size Chat"



# Example special workspace (scratchpad)
bindd = $mod, M, Ooohhh Magic, togglespecialworkspace, magic
bindd = $winMod, M, Send To Magic, movetoworkspace, special:magic

## }}}
##########################   MONITOR RELATED   ################### {{{
# Jump between monitors
bindd = $mod, LEFT,  Jump To Monitor 2, focusmonitor, $monitor2
bindd = $mod, DOWN,  Jump To Monitor 1, focusmonitor, $monitor1
bindd = $mod, RIGHT, Jump To Monitor 3, focusmonitor, $monitor3
# move window to montior
bindd = $winMod, LEFT,  Move Window To Monitor 2, movewindow, mon:$monitor2
bindd = $winMod, DOWN,  Move Window To Monitor 1, movewindow, mon:$monitor1
bindd = $winMod, RIGHT, Move Window To Monitor 3, movewindow, mon:$monitor3

# Move the current workspace to the next monitor
bindd = $powerMod, PERIOD, Move Workspace To Next Monitor, movecurrentworkspacetomonitor, +1

# move window to next monitor
bindd = $winMod, PERIOD, Move Window To Next Monitor, movewindow, mon:+1
## }}}
##########################   WINDOW CONTROL    ################### {{{
bindd = $mod, Q, Quit Window, killactive

# switch between all open windows
bindd = $mod, TAB, Switch Between All Windows, workspace, e+1
bindd = ALT, TAB, Switch To Previous Window, focuscurrentorlast
bind = $mod, F, cyclenext, floating


# change layout
bindd = $winMod, T, Toggle Tile/Float|layout, togglefloating
bindd = $mod, SLASH, Change Split, togglesplit

### Vimlike Window Focus
# Move focus with mainMod + raise the window

bind = $mod, H, movefocus, l
bind = $mod, H, alterzorder, top
bind = $mod, L, movefocus, r
bind = $mod, L, alterzorder, top
bind = $mod, K, movefocus, u
bind = $mod, K, alterzorder, top
bind = $mod, J, movefocus, d
bind = $mod, J, alterzorder, top

# Swap Windows
bind = $winMod, H, swapwindow, l
bind = $winMod, L, swapwindow, r
bind = $winMod, K, swapwindow, u
bind = $winMod, J, swapwindow, d

# show on all workspaces
bindd = $winMod, A, Show Window On All Workspaces|important every, pin

# move active above other windows
bindd = $mod, page_up, Move Active Window Above Others, bringactivetotop

### Window Control Size/Position
# bindd = $mod, F, Fullscreen, fullscreen, 1
bind = $winMod, F, fullscreen, 0

#Resize Window one day make a power window mover/resizer
# {{{
# https://wiki.hypr.land/Configuring/Binds/#submaps
# Switch to a submap called `resize`.
# TODO: make a move like this `move` and moveactive
bind = $winMod, R, submap, resize

bind = $winMod, right, resizeactive, 120 0
bind = $winMod, left, resizeactive, -120 0
bind = $winMod, up, resizeactive, 0 -120
bind = $winMod, down, resizeactive, 0 120


# Start a submap called "resize".
submap = resize

# Set repeatable binds for resizing the active window.
binde = , right, resizeactive, 10 0
binde = , left, resizeactive, -10 0
binde = , up, resizeactive, 0 -10
binde = , down, resizeactive, 0 10

binde = , l, resizeactive, 10 0
binde = , h, resizeactive, -10 0
binde = , k, resizeactive, 0 -10
binde = , j, resizeactive, 0 10

# Use `reset` to go back to the global submap
bind = , escape, submap, reset
# Reset the submap, which will return to the global submap
submap = reset
# }}}
## }}}
##########################   SPECIAL KEYS      ################### {{{

# Laptop multimedia keys for volume and LCD brightness
# bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
# bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
# bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bind = , XF86AudioNext, exec, playerctl next
bind = , XF86AudioPause, exec, playerctl play-pause
bind = , XF86AudioPlay, exec, playerctl play-pause
bind = , XF86AudioPrev, exec, playerctl previous

bindel = , XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +5%  && hyprctl notify -1 600 $colInfo "Volume" "▲"
bindel = , XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -5%  && hyprctl notify -1 600 $colInfo "Volume" "▼"
bindel = , XF86AudioMute,        exec, pactl set-sink-mute @DEFAULT_SINK@ toggle && hyprctl notify -1 600 $colInfo "Volume" "Mute toggled"

# top row of special buttons
# bind = , XF86HomePage
# bind = , XF86Search
# bind = , XF86Mail
# launch 1-5, starts @5...yeah, ok
# bind = , XF86Launch5
# bind = , XF86Launch6
# bind = , XF86Launch7
# bind = , XF86Launch8
# bind = , XF86Launch9
# bind = , XF86Favorites
bind = , XF86Calculator, $launch_focus "galculator" "galculator"

# bind = , Print
# bind = , Scroll_Lock
# bind = , Pause

# bind = , Insert
# bind = , Delete
# bind = , Home
# bind = , End
# page up/down
# bind = , Prior
# bind = , Next

# bind = , Super_R
# bind = , Super_L
# right menu button
# bind = , Menu
# bind = , XF86Back
# bind = , XF86Forward


# FX keys:
bindd = , Help, Hyprland Wiki, $launch_focus "Hyperland Wiki" "thorium-browser --app=https://wiki.hypr.land"
# bind = , Undo
# bind = , Redo
# bind = , XF86New
# bind = , XF86Open
# bind = , XF86Close
# bind = , XF86Reply
# bind = , XF86MailForward
# bind = , XF86Send
# bind = , XF86SpellCheck
# bind = , XF86Save
# bind = , Print
## }}}
##########################   MOUSE AROUND      ################### {{{

# Scroll through existing workspaces with mainMod + scroll
bind = $mod, mouse_down, workspace, e+1
bind = $mod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow

bind = $mod, N, movecursortocorner, 1
# movecursortocorner	moves the cursor to the corner of the active window	direction, 0 - 3, bottom left - 0, bottom right - 1, top right - 2, top left - 3
# movecursor	moves the cursor to a specified position	x y

## }}}
