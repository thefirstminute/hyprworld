exec-once = waybar & hyprpaper & hypridle & udiskie

source = ~/.config/hypr/vars.conf

# See https://wiki.hypr.land/Configuring/Monitors/
# monitor=,preferred,auto,auto

monitor=$monitor1_settings
monitor=$monitor2_settings
monitor=$monitor3_settings


#####################
###  KEYBINDINGS  ###
#####################
# {{{

## Important Commands:
bindu = $mod, RETURN, exec, $terminal

# Quit Hyprland:
bindu = $sysMod, Q, exec, hyprctl dispatch exit 0

# close current, then focus last
# bindd = $mod, Q, Quit Window, exec, hyprctl dispatch killactive && hyprctl dispatch focuscurrentorlast
# close current
bindd = $mod, Q, Quit Window, exec, hyprctl dispatch killactive

# Launcher:
bindd = $mod, D, Apps, exec, rofi -show combi -modes combi -combi-modes "window,drun"

### Vimlike Window Focus
# Move focus with mainMod + raise the window

bind = $mod, H, movefocus, l
bind = $mod, H, alterzorder, top
bind = $mod, L, movefocus, r
bind = $mod, L, alterzorder, top
bind = $mod, K, movefocus, u
bind = $mod, K, alterzorder, top
bind = $mod, J, movefocus, d
bind = $mod, J, alterzorder, top

# Swap Windows
bind = $winMod, H, swapwindow, l
bind = $winMod, L, swapwindow, r
bind = $winMod, K, swapwindow, u
bind = $winMod, J, swapwindow, d

# Switch workspaces
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9
bind = $mod, 0, workspace, 10

# Move active window to a workspace
bind = $winMod, 1, movetoworkspacesilent, 1
bind = $winMod, 2, movetoworkspacesilent, 2
bind = $winMod, 3, movetoworkspacesilent, 3
bind = $winMod, 4, movetoworkspacesilent, 4
bind = $winMod, 5, movetoworkspacesilent, 5
bind = $winMod, 6, movetoworkspacesilent, 6
bind = $winMod, 7, movetoworkspacesilent, 7
bind = $winMod, 8, movetoworkspacesilent, 8
bind = $winMod, 9, movetoworkspacesilent, 9
bind = $winMod, 0, movetoworkspacesilent, 10

source = ~/.config/hypr/keybinds.conf

# }}}

#####################
### LOOK AND FEEL ###
#####################
# {{{
# See https://wiki.hypr.land/Configuring/Environment-variables/
env = XDG_SESSION_TYPE=hyprland
$cursor_size = 14
$cursor_name = WinSur White Cursor

env = GTK_THEME=Vivid-Dark-GTK
env = GTK_ICON_THEME=Vivid-Dark-Icons
env = cursor=$cursor_name
env = gtk-cursor-theme-name=$cursor_name
env = gtk-cursor-theme-size=$cursor_size
env = XCURSOR_SIZE=$cursor_size
env = HYPRCURSOR_THEME,$cursor_name
env = HYPRCURSOR_SIZE,$cursor_size
env = QT_CURSOR_THEME,$cursor_name
env = QT_CURSOR_SIZE,$cursor_size

exec = hyprctl setcursor $cursor_name $cursor_size
# exec = gsettings set org.gnome.desktop.interface gtk-theme 'Vivid-Dark-GTK'
exec = gsettings set org.gnome.desktop.interface color-scheme 'prefer-dark'


source = ~/.cache/wal/colors-hyprland.css

# Refer to https://wiki.hypr.land/Configuring/Variables/

# https://wiki.hypr.land/Configuring/Variables/#general
general {
    gaps_in = 6
    gaps_out = 6
    border_size = 3

    # https://wiki.hypr.land/Configuring/Variables/#variable-types for info about colors
    # col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    # col.inactive_border = rgba(595959aa)

    col.active_border = $color6 $color7 22deg linear
    bezier = linear, 0.0, 0.0, 1.0, 1.0
    col.inactive_border = $background $color8 90deg

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hypr.land/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    no_focus_fallback = false

    layout = dwindle
}

# https://wiki.hypr.land/Configuring/Variables/#decoration
decoration {
    rounding = 8
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1
    inactive_opacity = 0.98

    shadow {
        enabled = true
        range = 20
        render_power = 8
        color = $color5
        color_inactive = $color0
    }

    # https://wiki.hypr.land/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1
        new_optimizations = true
        brightness = .4
        vibrancy = 0.1696
    }
}

# https://wiki.hypr.land/Configuring/Variables/#animations
animations {
# {{{
    enabled = true

    # "fast" from https://github.com/mylinuxforwork/dotfiles/blob/main/dotfiles/.config/hypr/conf/animations/animations-fast.conf
    bezier = linear, 0, 0, 1, 1
    bezier = md3_standard, 0.2, 0, 0, 1
    bezier = md3_decel, 0.05, 0.7, 0.1, 1
    bezier = md3_accel, 0.3, 0, 0.8, 0.15
    bezier = overshot, 0.05, 0.9, 0.1, 1.1
    bezier = crazyshot, 0.1, 1.5, 0.76, 0.92
    bezier = hyprnostretch, 0.05, 0.9, 0.1, 1.0
    bezier = fluent_decel, 0.1, 1, 0, 1
    bezier = easeInOutCirc, 0.85, 0, 0.15, 1
    bezier = easeOutCirc, 0, 0.55, 0.45, 1
    bezier = easeOutExpo, 0.16, 1, 0.3, 1
    animation = windows, 1, 3, md3_decel, popin 60%
    animation = border, 1, 10, default
    animation = fade, 1, 2.5, md3_decel
    animation = workspaces, 1, 3.5, easeOutExpo, slide
    animation = specialWorkspace, 1, 3, md3_decel, slidevert
# }}}
}

# "Smart gaps" / "No gaps when only"
# {{{
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule {
    # name = no-gaps-wtv1
    # match:float = false
    # match:workspace = w[tv1]
#
    # border_size = 0
    # rounding = 0
# }
#
# windowrule {
    # name = no-gaps-f1
    # match:float = false
    # match:workspace = f[1]
#
    # border_size = 0
    # rounding = 0
# }
# }}}

# See https://wiki.hypr.land/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hypr.land/Configuring/Master-Layout/ for more
master {
    allow_small_split = true
    special_scale_factor = 1
    mfact = 0.25
    new_status = slave
    new_on_top = false
    new_on_active = none
    orientation = top # center/left/right/top/bottom
    smart_resizing = true
    drop_at_cursor = true

}

# https://wiki.hypr.land/Configuring/Variables/#misc
misc {
    # wallpaper:
    force_default_wallpaper = 0
    disable_hyprland_logo = true
    disable_splash_rendering = true

    # wake up:
    mouse_move_enables_dpms = false
    key_press_enables_dpms = true

    middle_click_paste = true

    # do not reload config on save - could save resources, and if you're trying something crazy...
    # disable_autoreload = true
    disable_autoreload = false
}

debug {
    overlay = false
    damage_tracking = 2
    damage_blink = false
}

# }}}

#####################
###     INPUT     ###
#####################
# {{{

# https://wiki.hypr.land/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options = caps:swapescape,altwin:swap_alt_win
    kb_rules =
    follow_mouse = 1
    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
    touchpad {
      natural_scroll = false
    }
}

# See https://wiki.hypr.land/Configuring/Gestures
gesture = 3, horizontal, workspace

# Example per-device config
# See https://wiki.hypr.land/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}
# }}}

#####################
## WORKSPACE RULES ##
#####################
# {{{
# See https://wiki.hypr.land/Configuring/Workspace-Rules/ for workspace rules
workspace = 1, monitor:$monitor2
workspace = 2, monitor:$monitor2
workspace = 3, monitor:$monitor2
workspace = 4, monitor:$monitor2
workspace = 5, monitor:$monitor1
workspace = 6, monitor:$monitor1
workspace = 7, monitor:$monitor3
workspace = 8, monitor:$monitor3
workspace = 9, monitor:$monitor3
workspace = 10, monitor:$monitor3

workspace = 11, name:Why, monitor:$monitor1, layout master
workspace = 12, name:Upload, monitor:$monitor2
workspace = 13, name:Code, gapsin:1, gapsout:2, monitor:$monitor1, layout master
# workspace = 14, name:Online, on-created-empty: firefox, monitor:$monitor3
workspace = 14, name:Online, monitor:$monitor3
workspace = 15, name:PR0, monitor:$monitor1
workspace = 16, name:Draft, monitor:$monitor3
workspace = 17, name:Utils, on-created-empty: kitty -e btop, monitor:$monitor2
workspace = 18, name:Nota, monitor:$monitor3
workspace = 19, name:Chat, monitor:$monitor1
workspace = 20, name:Tube, monitor:$monitor2, on-created-empty: freetube

# }}}

#####################
###  WINDOW RULES ###
#####################
source = ~/.config/hypr/window-rules.conf
